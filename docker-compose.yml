version: "3.7"
services:
    prestashop:
        build: 
            dockerfile: $PWD/202/docker/Dockerfile
            context: .
            args:
              - RUN_USER=clotaire
        environment:
            PS_DOMAIN: "172.28.0.2"
            RUN_USER: clotaire
        ports:
            - 8080:80
            - 8443:443
        volumes:
            - $PWD:/var/www/html/modules/shoppingfeed
volumes:
  data:

